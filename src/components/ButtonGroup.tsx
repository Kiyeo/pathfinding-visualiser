import React from "react";
import styled from "styled-components";
import Button from "./Button";

const ButtonGroup = styled.nav``;

export default () => {
  return (
    <ButtonGroup>
      <Button>Generate Weight</Button>
    </ButtonGroup>
  );
};
